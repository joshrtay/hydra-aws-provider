{
  "name": "hydra-aws-provider",
  "description": "Hydra helper for adding aws provider conifg.",
  "repository": "git://github.com/joshrtay/hydra-aws-provider.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "aws-read-region": "^1.1.0",
    "lambda-json": "^1.0.1"
  },
  "devDependencies": {
    "co": "^4.6.0",
    "hydraform": "^0.1.5",
    "tape": "^4.2.2"
  }
}
